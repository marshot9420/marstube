doctype html 
html(lang="ko")
    head
        include partials/meta
        include partials/meta-og
        //- 모바일 구현 후 모바일 앱 미리보기 설정 meta 태그 (al:ios, al:android) 추가
        title #{pageTitle} | #{siteName}
        block head
    body
        header 
            h1=pageTitle 
        main 
            block content 
        include partials/footer